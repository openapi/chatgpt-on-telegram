services:
  chatgpt-on-telegram:
    build: .
    ports:
      - "8000:8000"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:?required}
      SERVER_SECRET_KEY: ${SERVER_SECRET_KEY:?required}
    restart: unless-stopped
